const coffee = require('./5.Modularization'); // Parameter merupakan lokasi dari module target import
 
console.log(coffee);

// hendak mengimpor modul lokal (local module), selalu gunakan tanda (./)
// Anda juga bisa menggunakan tanda (../) untuk keluar dari satu level folder
